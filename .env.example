# Velora environment example
# Copy this file to .env.local (never commit .env.local) and fill in values.
# For production, set real secrets in the hosting provider or on the server.

# --- Database (Prisma) ---
# PostgreSQL connection string used by Prisma in prisma/schema.prisma
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME?schema=public
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public

# --- NextAuth ---
# Secret key for NextAuth (generate one: `openssl rand -base64 32`)
NEXTAUTH_SECRET=change_me

# GitHub OAuth app credentials (NextAuth provider)
AUTH_GITHUB_ID=your_github_client_id
AUTH_GITHUB_SECRET=your_github_client_secret

# --- Public App URL ---
# Used to generate absolute URLs in metadata and sitemaps
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Maps / Geocoding ---
# Public key used on the client for LocationIQ (treat with care)
NEXT_PUBLIC_LOCATION_IQ_API_KEY=your_locationiq_public_key

# --- Optional ---
# Explicitly set the Node environment if needed
# NODE_ENV=development
